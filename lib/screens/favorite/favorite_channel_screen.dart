import 'package:flutter/material.dart';

class FavoriteChannelScreen extends StatelessWidget {
  const FavoriteChannelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Favorite Channel Screen'),
      ),
    );
  }
}
